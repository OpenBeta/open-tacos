[phases.setup]
nixPkgs =  ["nodejs_20", "yarn_1_22_22"]

[phases.install]
cmds = ["yarn install --no-progress --silence"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn start"

[variables]
NODE_ENV = 'production'
DEPLOYMENT_ENV = 'railway'
