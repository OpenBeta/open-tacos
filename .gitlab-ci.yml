cache:
  paths:
    - node_modules/

image: node:alpine3.13

pages:
  script:
    - yarn install
    - ./prebuild.sh full # fetch content repo
    - ./node_modules/.bin/gatsby build
  artifacts:
    paths:
      - public