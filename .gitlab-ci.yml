cache:
  paths:
    - node_modules/

image: node:16.3-buster-slim

pages:
  script:
    - yarn install
    - ./prebuild.sh full # fetch content repo
    - ./node_modules/.bin/gatsby build
  artifacts:
    paths:
      - public